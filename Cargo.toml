[package]
name = "rust_scheduler_demo"
version = "0.1.0"
edition = "2024"

[dependencies]
aarch32-cpu = "0.3.0"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
debug = true
lto = "thin"
